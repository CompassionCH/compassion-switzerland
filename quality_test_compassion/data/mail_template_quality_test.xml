<?xml version="1.0" encoding="utf-8" ?>
<odoo noupdate="1">
    <record
      id="email_template_quality_test_notification"
      model="mail.template"
    >
        <field name="name">Quality Test – Run Required</field>
        <field
      name="model_id"
      ref="quality_test_compassion.model_quality_test"
    />
        <field
      name="subject"
    >Action Required: Quality Test "{{ object.name }}" needs a new run</field>
        <field name="use_default_to" eval="True" />
        <field name="body_html" type="html">
<div
          style="font-family: Arial, sans-serif; font-size: 14px; line-height: 1.6; color: #333;"
        >
    <p>Hello {{ object.user_id.name }},</p>
    <p>
        The quality test <strong
            >{{ object.name }}</strong> requires a new test run
        for the following reason(s):
    </p>
    <ul>
        {% for reason in ctx.get('notification_reasons', []) %}
            <li>{{ reason }}</li>
        {% endfor %}
    </ul>
    {% if object.step_ids %}
        <p><strong>Test Steps:</strong></p>
        <ol style="margin-left: 1em;">
            {% for step in object.step_ids %}
                <li>{{ step.name }}</li>
            {% endfor %}
        </ol>
    {% endif %}
    <p>
        Please log in to Odoo and record a new test run as soon as possible.
    </p>
    <p>Best regards,<br />Compassion Switzerland – Quality Team</p>
</div>
        </field>
        <field name="auto_delete" eval="True" />
    </record>
    <record id="email_template_quality_test_run_fail" model="mail.template">
        <field name="name">Quality Test – Failing Run Recorded</field>
        <field
      name="model_id"
      ref="quality_test_compassion.model_quality_test_run"
    />
        <field
      name="subject"
    >Failing Quality Test Run: "{{ object.test_id.name }}"</field>
        <field name="email_to">{{ object.test_id.user_id.email }}</field>
        <field name="body_html" type="html">
<div
          style="font-family: Arial, sans-serif; font-size: 14px; line-height: 1.6; color: #333;"
        >
    <p>Hello {{ object.test_id.user_id.name }},</p>
    <p>
        A test run for <strong>{{ object.test_id.name }}</strong> recorded on
        <strong>{{ object.date }}</strong> has <span
              style="color: #d9534f;"
            ><strong>failed</strong></span>.
    </p>
    {% if object.failed_result_ids %}
        <p><strong>Failed expected results:</strong></p>
        <ul style="margin-left: 1em;">
            {% for line in object.failed_result_ids %}
                <li>
                    <strong>{{ line.step_name }}</strong>: {{ line.name }}
                    {% if line.comment %}<br />{{ line.comment }}{% endif %}
                </li>
            {% endfor %}
        </ul>
    {% endif %}
    {% if object.comment %}
        <p><strong>Notes:</strong></p>
        <div style="margin-left: 1em;">{{ object.comment }}</div>
    {% endif %}
    <p>
        Please investigate and record a passing run once the issue is resolved.
    </p>
    <p>Best regards,<br />Compassion Switzerland – Quality Team</p>
</div>
        </field>
        <field name="auto_delete" eval="True" />
    </record>
</odoo>
