<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0"> <!-- TODO: change to 1 when finished -->
        <record id="group_criminal_record" model="res.groups">
            <field name="name">Manage Criminal Records</field>
            <field name="comment">the user will have access to all criminal records.</field>
            <field name="users" eval="[(4, ref('base.user_admin'))]"/>
        </record>
    </data>
</odoo>