Adds monitoring for scheduled and automated actions, logging their execution time and any errors.
Provides a search view for filtering and grouping logs by server action, user, or exception.
Stores execution duration as both a formatted string and a sortable float (seconds) for efficient analysis.
Ensures logs are saved even if the main transaction is rolled back.
Includes access control for user and admin roles.
