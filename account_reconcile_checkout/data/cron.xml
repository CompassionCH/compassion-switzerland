<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="cron_reconcile_postfinance" model="ir.cron">
        <field name="name">automatic PostFinance reconciliation</field>
        <field name="model_id" ref="model_postfinance_reconcile_wizard"/>
        <field name="state">code</field>
        <field name="code">model.action_reconcile()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="nextcall" eval="(DateTime.now() + timedelta(days=1)).replace(hour=2, minute=0, second=0)"/>
        <field name="numbercall">-1</field>
    </record>
</odoo>