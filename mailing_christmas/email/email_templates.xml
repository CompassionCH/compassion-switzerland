<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>
        <!-- Christmas fund email -->
        <record id="email_christmas_fund" model="email.template">
            <field name="name">Christmas funds email</field>
            <field name="model_id" search="[('model','=','res.partner')]"/>
            <field name="email_from">${object.company_id.email}</field>
            <field name="email_to">${object.email}</field>
            <field name="partner_to">${object.id}</field>
            <field name="subject">Your gift to illuminate a Christmas on the other side of the world</field>
            <field name="body_html" type="html">
                <div>
                    % if len(object.contracts_fully_managed) == 0:
                    %    set phrase = 'Maybe you want to sponsor a child?'
                    % else:
                    %    set phrase = 'You will make your children happy'
                    % endif
                    % if len(object.contracts_fully_managed) == 1:
                    %    set phrase = 'You will make {} happy.'.format(object.contracts_fully_managed[0].child_id.firstname)
                    % endif
                    % set format = {'Maybe you want to sponsor a child?': 'Maybe you want to sponsor a child?',
                                    'You will make Herbert happy.': 'You will make Herbert happy.',
                                    'You will make your children happy': 'You will make your children happy'}
                </div>
                <p>Dear ${object.title.name or ''} ${object.name},</p>
                <p>Compassion whishes you merry christmas etc.</p>
                <p>${phrase}</p>
                <p>Yours sincerely</p>
            </field>
            <field name="report_template" ref="christmas_bvr_report"/>
        </record>
    </data>
</openerp>
