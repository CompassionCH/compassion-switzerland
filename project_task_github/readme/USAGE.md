Start the title of a pull request with the code of the task, or name its branch
after it, and the **PR URI** field of the task is filled in when the pull
request is opened.

Pull requests that carry no task code are ignored, as are the tasks that
already have a PR URI.
